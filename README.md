# Top 3 Files Finder

A Python script to find the top 3 largest files in a directory.

## Usage

```bash
python list_top_files.py <directory_path>
```

Replace `<directory_path>` with the path to the directory you want to analyze.

The script will output a table with the top 3 largest files, their sizes in a human-readable format, and their paths.
